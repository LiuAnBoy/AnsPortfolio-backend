import { Layout } from 'antd';

const { Header } = Layout;

function HeaderBar() {
  return (
    <Header>
      <div className="Logo text-white">An's Portfolio</div>
    </Header>
  );
}

export default HeaderBar;
